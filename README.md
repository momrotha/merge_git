
#Command push git 
```bash

    git init
    git add .
    git commit -m "create : home page"
    git remote
    git push origin main
    # check branch 
    git branch
    git branch -r
    # change branch
    git checkout -b rotha
    git add .
    git commit -m ""
    git push origin rotha
    # git merge
    git merge rotha
    :wq
    git push origin main
```